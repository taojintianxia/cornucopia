spring:
  application:
    name: ribbon-client

server:
  port: 8888

hello-world-service:
  ribbon:
    eureka:
      enabled: false
    listOfServers: localhost:8090,localhost:8091,localhost:8092
    ServerListRefreshInterval: 15000
